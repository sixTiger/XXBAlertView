Pod::Spec.new do |s|
  s.name             = "XXBAlertView"
  s.version          = "1.0.0"
  s.author       = { "杨小兵" => "six_tiger@163.com" }
  s.platform     = :ios, '7.0'
  s.source_files = "XXBAlertView"
  s.requires_arc = true
end
